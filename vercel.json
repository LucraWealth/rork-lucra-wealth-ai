{
  "buildCommand": "rm -rf node_modules bun.lockb .expo package-lock.json yarn.lock && bun pm cache rm && bun install && bunx expo-doctor && bun expo publish --clear && expo export -p web --clear",
  "outputDirectory": "dist",
  "devCommand": "expo",
  "cleanUrls": true,
  "framework": null,
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/"
    }
  ]
}