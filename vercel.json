{
  "buildCommand": "rm -rf node_modules bun.lockb .expo && bun pm cache rm && bun install && expo export -p web --clear",
  "outputDirectory": "dist",
  "devCommand": "expo start",
  "cleanUrls": true,
  "framework": null,
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/"
    }
  ]
}