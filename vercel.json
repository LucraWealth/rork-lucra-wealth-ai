{
		"buildCommand": "rm -rf node_modules bun.lockb .expo && bun install && expo export -p web --clear",  
    "outputDirectory": "dist",
    "devCommand": "expo",
    "cleanUrls": true,
    "framework": null,
    "rewrites": [
      {
        "source": "/:path*",
        "destination": "/"
      }
    ]
}