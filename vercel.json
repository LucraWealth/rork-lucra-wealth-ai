{
	"buildCommand": "rm -rf node_modules bun.lockb .expo yarn.lock && bun pm cache rm && bun install && expo export -p web",  
  "outputDirectory": "dist",
  "devCommand": "expo",
  "cleanUrls": true,
  "framework": null,
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/"
    }
  ]
}