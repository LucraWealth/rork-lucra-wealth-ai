{
	"buildCommand": "rm -rf node_modules bun.lockb .expo yarn.lock && bun install && bun expo publish --clear && expo export -p web --clear",  
  "outputDirectory": "dist",
  "devCommand": "expo",
  "cleanUrls": true,
  "framework": null,
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/"
    }
  ]
}